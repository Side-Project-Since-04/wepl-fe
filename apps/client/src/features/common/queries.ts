import { budgetKeys } from '../budget/queries';
import { classificationKeys } from '../classification/queries';
import { weddingKeys } from '../wedding/queries';

export const queries = {
  budget: budgetKeys,
  classification: classificationKeys,
  wedding: weddingKeys,
};
